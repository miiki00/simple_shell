#include "main.h"

/**
 * init_paths - initializes a linked list of path directores
 * which are in the enviromental variable of a process.
 *
 * Return: 1 (success), -1 (error).
 */
int init_paths(void)
{
	
}
